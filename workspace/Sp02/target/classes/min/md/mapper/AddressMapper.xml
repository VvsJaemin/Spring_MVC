<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="min.md.mapper.AddressMapper">
	<select id="list" resultType="Address">

		SELECT * FROM Address order by SEQ
		desc

	</select>

	<insert id="insert" parameterType="Address"
		useGeneratedKeys="true" keyProperty="seq">
		<selectKey keyProperty="seq" resultType="long"
			order="BEFORE">

			SELECT ADDRESS_SEQ.nextval from DUAL

		</selectKey>

		insert into Address
		values(ADDRESS_SEQ.nextval, #{name}, #{addr},
		SYSDATE)


	</insert>

	<insert id="files" parameterType="Address">


		insert into address_file
		(SEQF, SEQ, OFNAME, SFNAME, FSIZE)
		values(ADDRESS_FILE_SEQF.nextval,
		#{SEQ}, #{OFNAME}, #{SFNAME}, #{FSIZE})

	</insert>



	<delete id="delete" parameterType="long">

		delete from Address WHERE
		SEQ=#{SEQ}

	</delete>
</mapper>